# @hrms/shared-types

This package contains OpenAPI-generated TypeScript types shared across the Zero-Trust HRMS frontend applications (`web-employee` and `web-admin`).

## Purpose
By auto-generating TypeScript interfaces directly from the FastAPI OpenAPI schema, we guarantee that the frontend clients and backend models remain perfectly in sync.

Do not edit files in this package manually. They will be generated by the API schema generation script.

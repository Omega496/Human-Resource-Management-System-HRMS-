[project]
name = "automation"
version = "0.1.0"
description = "Zero-Trust HRMS Automation Service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.6.3",
    "httpx>=0.28.1",
    "playwright>=1.49.0",
    "pydantic>=2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
